[project]
name = "smart-farming-openenv"
version = "1.1.1"
dependencies = [
    "openenv-core>=0.2.0",
    "openai",
    "fastapi",
    "uvicorn"
]

[project.scripts]
server = "server.app:main"

[tool.setuptools]
py-modules = ["tasks"]
